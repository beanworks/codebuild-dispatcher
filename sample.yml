- type: serial/parallel
  name: string
  branch-include: regex // the following three are and relationships
  branch-exclude: regex
  path-include: regex
  if it's parallel
  steps:
  - name:
    branch-include:
    branch-exclude:
    path-include:
    commands: 
      - command 1
      - command 2 // build commands to run
  